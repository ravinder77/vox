# ─────────────────────────────────────────────────────────────────
# backend/sonar-project.properties
# Place this file inside the backend/ directory.
# ─────────────────────────────────────────────────────────────────

sonar.projectKey=vox-backend
sonar.projectName=vox — Backend (Node.js)
sonar.projectVersion=1.0

# Source & test directories
sonar.sources=src
sonar.tests=src
sonar.test.inclusions=**/*.test.ts,**/*.spec.ts,**/*.test.js,**/*.spec.js
sonar.exclusions=node_modules/**,dist/**,coverage/**,**/*.d.ts

# Coverage report (produced by Jest --coverage)
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.coverage.exclusions=**/*.test.ts,**/*.spec.ts,**/__mocks__/**

# Language
sonar.language=js
sonar.sourceEncoding=UTF-8

# Quality Gate — fail PR if new code has issues
sonar.qualitygate.wait=true